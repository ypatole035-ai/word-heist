// This component is no longer used — intel is shown inline in PuzzleArea while solving.
// Kept as empty stub to avoid import errors during transition.
export default function WordDescription() {
  return null;
}
